Mutation Survivor routine for 2026-06-10 could not run.

Picked: src/core/round.ts (top of rotation).

Blocker: wrap-core resolves to github#main (3de07ca), which lacks the
`wrap-core/chrome` and `wrap-core/text` exports added in wrap commit
e569c80 (2026-06-02). 74 tests fail at import time; stryker bails before
mutation.

Secondary: `vault/wrap-core-api` is a broken symlink in the routine env
(targets a sibling checkout that doesn't exist here).

See PR body for details, repro, and proposed fixes.
