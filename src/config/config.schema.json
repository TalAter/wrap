{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Wrap configuration file",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "maxRounds": {
      "type": "integer",
      "description": "Maximum number of LLM rounds (probes + error-fix attempts) per invocation. Default: 5.",
      "minimum": 1,
      "default": 5
    },
    "maxProbeOutputChars": {
      "type": "integer",
      "description": "Maximum characters of probe command output sent to the LLM. Longer output is truncated. Default: 200000 (~200KB).",
      "minimum": 1000,
      "default": 200000
    },
    "maxPipedInputChars": {
      "type": "integer",
      "description": "Maximum characters of piped stdin sent to the LLM. Longer input is truncated but the full content is still available for re-piping to commands. Default: 200000 (~200KB).",
      "minimum": 1000,
      "default": 200000
    },
    "verbose": {
      "type": "boolean",
      "description": "Enable verbose mode. Surfaces internal pipeline steps on stderr.",
      "default": false
    },
    "providers": {
      "type": "object",
      "description": "Map of provider name → entry. Known names: anthropic, openai, ollama, claude-code. Any other key is treated as an OpenAI-compatible endpoint and requires baseURL, apiKey, and model.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "apiKey": {
            "type": "string",
            "description": "API key. Use \"$ENV_VAR\" to read from environment. Omit for the SDK's default env var (e.g. ANTHROPIC_API_KEY)."
          },
          "baseURL": {
            "type": "string",
            "description": "Custom API endpoint (required for ollama and OpenAI-compatible providers)."
          },
          "model": {
            "type": "string",
            "description": "Model identifier the provider should use."
          }
        },
        "additionalProperties": false
      }
    },
    "defaultProvider": {
      "type": "string",
      "description": "Name of the entry in `providers` to use by default."
    }
  },
  "additionalProperties": false
}
